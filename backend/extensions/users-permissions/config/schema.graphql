module.exports = {
  definition: `
    type MyProjects {
      id: ID!
      uid: String
      name: String
    }
    type MyProject {
      name: String
    }
  `,
  query: `
    myProjects(email: String): [MyProjects]
    myProject(email: String, id: ID!): MyProject
  `,

  resolver: {
    Query: {
      myProjects: {
        description: 'Get user project Info',
        resolver: 'plugins::users-permissions.user.getProjects'
      },
      myProject: {
        description: 'Get user project Info',
        resolver: 'plugins::users-permissions.user.getProject'
      }
    }
  }
}